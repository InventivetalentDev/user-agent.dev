const http = require("http");
http.request({
    host: "%%urlhost%%",
    path: "%%urlpath%%"
}, function(res) {
    res.setEncoding('utf8');
    res.on('data', function (chunk) {
        console.log(chunk);
    });
}).end();
